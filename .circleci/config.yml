version: 2.1
aliases:
  - &defaults
      docker:
        - image: circleci/openjdk:8-jdk
jobs:
    build_demo:
      <<: *defaults
      steps:
        - checkout
        - run:
            name: Saving HEAD checksum
            command: cd demo && echo "$(git rev-parse HEAD)" > /tmp/head_checksum
        - restore_cache:
            name: "Retrieving cached dependencies"
            keys:
              - m2-demo-{{ checksum "demo/pom.xml" }}
        - run:
            name: Building Demo
            command: |
              cd demo/;
                mvn clean package -DskipTests dependency:resolve-plugins dependency:go-offline
        - save_cache:
            name: "Caching dependencies"
            paths:
              - ~/.m2
            key: m2-demo-{{ checksum "demo/pom.xml" }}

orbs:
  maven: circleci/maven@0.0.12

workflows:
  version: 2
  build:
    jobs:
      - build_demo
